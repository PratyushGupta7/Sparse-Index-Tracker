# =============================================================================
# Sparse Index Tracker — development tooling
# Install with:  pip install -r requirements-dev.txt
# =============================================================================

# Linting + formatting
ruff>=0.6,<1.0
black>=24.4,<26.0
mypy>=1.10,<2.0
pre-commit>=3.7,<5.0

# Testing
pytest>=8.2,<9.0
pytest-cov>=5.0,<8.0
pytest-xdist>=3.6,<4.0          # parallel test execution
pytest-timeout>=2.3,<3.0
pytest-randomly>=3.15,<4.0      # surface order-dependent tests
hypothesis>=6.108,<8.0          # property-based testing for invariants

# Type stubs
types-requests>=2.32,<3.0
pandas-stubs>=2.2,<3.0

# Notebooks / interactive
jupyterlab>=4.2,<5.0
ipykernel>=6.29,<7.0
ipywidgets>=8.1,<9.0

# Docs / utilities
tabulate>=0.9,<1.0
